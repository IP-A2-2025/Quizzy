spring:
  datasource:
    url: jdbc:postgresql://localhost:${TESTCONTAINERS_POSTGRESQL_PORT}/test_db
    username: postgres
    password: yourpassword
  jpa:
    hibernate:
      ddl-auto: update  # Folosește `update` pentru a crea automat tabelele
    show-sql: true
    properties:
      hibernate:
        format_sql: true
